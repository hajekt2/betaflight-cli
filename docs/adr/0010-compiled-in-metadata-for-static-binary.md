# Compiled-in metadata for the static binary

Generated Betaflight protocol, setting, and compatibility metadata for supported releases will be compiled into the single static binary.
Normal operation should not require network access or a mutable local metadata cache because agent workflows and cross-platform distribution need predictable behavior.
An optional external metadata update mechanism can be added later as an advanced feature.
