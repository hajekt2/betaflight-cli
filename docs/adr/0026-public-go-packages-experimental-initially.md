# Public Go packages experimental initially

Initial public Go packages such as `pkg/msp` and `pkg/blackbox` will be experimental.
The CLI binary and JSON Response Envelope are the product contracts that should stabilize first.
Stabilizing library APIs too early would slow down protocol, metadata, and Blackbox design while Betaflight `2025.12.x` support matures.
