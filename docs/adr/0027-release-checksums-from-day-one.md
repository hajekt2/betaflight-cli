# Release checksums from day one

Release artifacts will include SHA256 checksums from day one.
`betaflight-cli` controls hardware, so users and agents should be able to verify downloaded binaries.
Signing and provenance should follow after the initial release process is reliable.
