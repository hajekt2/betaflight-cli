# No telemetry or analytics

`betaflight-cli` will not collect telemetry or analytics.
The tool operates around hardware, serial ports, and configuration data, so trust and predictability matter more than usage metrics.
Diagnostics are explicit user-controlled command outputs such as `doctor --json`.
